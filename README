Imager::File::TIFF provides TIFF file format support for Imager.

It requires libpng to be installed.

This is currently shipped as part of Imager, but in the future it will
be removed from Imager, if your module or application requires TIFF
support then add Imager::File::TIFF as a prerequisite.
